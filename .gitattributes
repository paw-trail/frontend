# 모든 텍스트 파일은 저장소에 LF 로 저장하고 체크아웃할 때도 LF 로 내림
# 윈도우에서 CRLF 로 받으면 도구마다 줄끝 처리가 달라 불필요한 변경이 생김
* text=auto eol=lf

# CRLF 면 환경변수 값 끝에 \r 이 붙어 키가 조용히 틀어짐
.env*   text eol=lf

# 바이너리는 손대지 않음
*.png   binary
*.jpg   binary
*.jpeg  binary
*.ico   binary
*.woff  binary
*.woff2 binary
