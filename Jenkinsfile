// 파이프라인 본체는 Jenkins 공유 라이브러리(paw-trail/jenkins-library)의 frontendPipeline 에 있습니다.
// 릴리스 태그(vX.Y.Z)를 달면 빌드한 화면이 https://paw-trail.click 에 올라갑니다.
// 빌드 값(VITE_…)은 레포가 아니라 Jenkins 자격 증명 frontend-env 에 있습니다.

@Library('pawtrail-pipeline') _

frontendPipeline()
